enum Direction {
  Up,
  Right,
  Down,
  Left,
}

console.log(Direction[0]);

class Zmogus {
  vardas: string;
  constructor(vardas: string) {
    this.vardas = vardas;
  }
}

let kintamasis: string = "stringas";
